image: gvm_builder
matrix:
  - env: GO_VERSION=1.10
  - env: GO_VERSION=1.11 GO111MODULE=on

cache:
  - /home/abstruse/.gvm/versions

before_install:
  - gvm install $GO_VERSION

install:
  - go get ./...

script:
  - go test ./...